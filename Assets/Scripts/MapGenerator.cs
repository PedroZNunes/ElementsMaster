using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapGenerator : MonoBehaviour {

	//gapMaxSize = (bounds.size.x * 0.9f) + maxJumpLength;
    //gapSizeMax = 10 is great for 12ms, 4max height , 0.25 timetoapex
}
